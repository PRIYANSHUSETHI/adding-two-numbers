# adding-two-numbers

#include <stdio.h>

int main()
{
int a,b;
printf("enter two numbers:");
scanf("%d",&a);
scanf("%d",&b);
    printf("the addition of the two numbers is:%d\n", a+b);

    return 0;
}
